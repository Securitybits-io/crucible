---
# dconf requires a database rebuild after system default files change.
- name: Update dconf database
  ansible.builtin.command:
    cmd: dconf update
  changed_when: true

- name: Restart systemd-logind
  ansible.builtin.service:
    name: systemd-logind
    state: restarted
